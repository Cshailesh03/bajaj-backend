{
  "name": "backend",
  "version": "1.0.0",
  "main": "api/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node api/app.js",
    "build": "echo \"No build step required for Node.js backend\"",
    "dev": "node api/app.js", 
    "lint": "eslint .",
    "prestart": "npm install",
    "clean": "rm -rf node_modules && npm install",
    "deploy": "echo \"Deploy command placeholder\" && exit 0"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "base64-img": "^1.0.4",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "is-prime": "^1.0.1",
    "mime-types": "^2.1.35",
    "multer": "^1.4.5-lts.1",
    "nodemon": "^3.1.7"
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "eslint-plugin-node": "^11.1.0"
  }
}
